spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Quiz
    password: Larry123
    username: postgres

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

    hibernate:
      dialect:
      ddl-auto: update


    show-sql: true

springdoc:
  swagger-ui:
    path=/swagger-ui:
      html: